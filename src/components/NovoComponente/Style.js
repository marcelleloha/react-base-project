import styled from "styled-components";

const Componente = styled.comp`
    h1{
    margin: 0;
    display: block;
    text-align: center;
    line-height: 3rem;
    text-transform: uppercase;
    background-color: var(--primaria);
    color: var(--secundaria);
    }
`
export {Componente};